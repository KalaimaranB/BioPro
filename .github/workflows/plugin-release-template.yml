name: Karcytics Plugin Release Template

on:
  workflow_call:
    secrets:
      KARCYTICS_PROJECT_PRIVATE_KEY:
        required: true
        description: "The Ed25519 private key used to sign the plugin manifest. Registered as a Project Entity in the Karcytics Trust System."

jobs:
  validate-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Install dependencies
        run: uv sync

      - name: Run Test Suite
        run: uv run pytest

      - name: Validate Plugin Structure
        run: uv run karcytics sdk evaluate .

      - name: Setup Project Signing Key
        run: |
          mkdir -p ~/.biopro
          echo "${{ secrets.KARCYTICS_PROJECT_PRIVATE_KEY }}" > ~/.biopro/dev_private_key.pem
          chmod 600 ~/.biopro/dev_private_key.pem
          # Initialize the local chain and cert using the project key
          uv run karcytics sdk init-identity --project

      - name: Sign Plugin Release
        run: |
          uv run karcytics sdk sign .

      - name: Package Release
        run: |
          PLUGIN_ID=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['tool']['karcytics']['plugin']['id'])")
          zip -r ${PLUGIN_ID}.zip . -x "*.git*" -x "*__pycache__*" -x "*.venv*"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: "*.zip"
